# Build Configuration
# This file defines default settings for RPM package builds

# Container image configuration for build workflows
# NOTE: With akmod pattern, we only need basic RPM build tools
# The akmods service on user systems handles kernel-specific builds
CONTAINER_IMAGE=fedora
CONTAINER_VERSION=latest
